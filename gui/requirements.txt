# Зависимости для GUI приложения ЦРПТ Маркировка

# Основная GUI библиотека
PyQt6>=6.6.0

# Дополнительные компоненты PyQt6 (если потребуются)
PyQt6-Qt6>=6.6.0
PyQt6-sip>=13.6.0

# Библиотеки для работы с данными и файлами
pandas>=2.0.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# Библиотеки для работы с изображениями (для иконок и диаграмм)
Pillow>=10.0.0

# Библиотеки для создания графиков и диаграмм
matplotlib>=3.7.0
seaborn>=0.12.0

# Дополнительные зависимости (уже должны быть установлены для основной системы)
requests>=2.31.0
colorama>=0.4.6
cryptography>=41.0.0
python-docx>=0.8.11
markdown>=3.5.0

# Для Windows-специфичных функций
pywin32>=306; sys_platform == "win32"

# Для работы с датами и временем
pytz>=2023.3

# Для логирования и конфигурации
pyyaml>=6.0.1

# Для тестирования GUI (опционально)
pytest-qt>=4.2.0; extra == "test"
